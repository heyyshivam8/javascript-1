//7.Write a program that takes a number and prints the multiplication table for that number.
function table(mul){
    for(let i=1;i<=10;i++)
    {
        let result = mul*i;
        console.log(`${result}`);

    }
}
console.log(table(5));
